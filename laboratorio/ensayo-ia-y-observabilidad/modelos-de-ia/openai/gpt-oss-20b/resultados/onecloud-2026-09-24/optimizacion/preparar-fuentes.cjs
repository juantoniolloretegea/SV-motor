const fs=require('fs'),p=require('path');const d=__dirname;
let s=fs.readFileSync(p.resolve(d,'../../auditoria-memoria/diagnostico-20260924/MOTOR_MXFP4_CORREGIDO.rs'),'utf8');
const start=s.indexOf('    fn gather_forward_dequantize(&self, x: &Tensor, indices: &Tensor) -> Result<Tensor> {');if(start<0)throw Error('función ausente');const end=s.indexOf('\n}\n\nimpl QuantizedSerde',start);let ref=s.slice(start,end).replace('fn gather_forward_dequantize','fn gather_forward_referencia').replace('        let x_dims = x.dims();','        let inicio = std::time::Instant::now();\n        let x_dims = x.dims();').replace('        // output: [num_tokens * topk, n]','        let preparado = inicio.elapsed();\n        // output: [num_tokens * topk, n]').replace('        let result = Tensor::from_vec(output,','        let calculado = inicio.elapsed();\n        if std::env::var_os("EIO_MXFP4_PERFIL").is_some() {\n            eprintln!("{{\\\"evento\\\":\\\"perfil_mxfp4\\\",\\\"modo\\\":\\\"referencia\\\",\\\"tokens\\\":{num_tokens},\\\"expertos_seleccionados\\\":{topk},\\\"filas\\\":{n},\\\"dimension\\\":{k},\\\"preparacion_ns\\\":{},\\\"calculo_ns\\\":{}}}", preparado.as_nanos(), (calculado-preparado).as_nanos());\n        }\n        let result = Tensor::from_vec(output,');
s=s.slice(0,start)+fs.readFileSync(d+'/funcion_paralela.rs','utf8')+'\n'+ref+s.slice(end)+fs.readFileSync(d+'/pruebas_paralelas.rs','utf8');fs.writeFileSync(d+'/MOTOR_INSTRUMENTADO.rs',s);
